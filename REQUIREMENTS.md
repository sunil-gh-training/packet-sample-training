# Requirements brief — release 1.0

R1. The displayed version in `version.txt` must be **1.0** for this release.
R2. The output label printed by the sample tool must read **Packet Sample**.
R3. Documentation in `docs/operations.md` must describe how to run the sample.
